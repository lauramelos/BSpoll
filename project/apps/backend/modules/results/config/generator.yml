generator:
  class: sfDoctrineGenerator
  param:
    model_class:           slides
    theme:                 mooDoo.2
    non_verbose_templates: true
    with_show:             false
    singular:              ~
    plural:                ~
    route_prefix:          slides
    with_doctrine_route:   true
    actions_base_class:    sfActions

    config:
      actions: ~
      fields:  ~
      list:
        actions:
          _new:                 { label: '&nbsp;', atts: {title: Nueva Encuesta } }
          excel:                { label: '&nbsp;', atts: {title: Exportar a Excel } }
      filter:  ~
      form:    ~
      edit:    ~
      new:     ~
